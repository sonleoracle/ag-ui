"""Runtime-specific runners for Agent‑Spec → AG‑UI integration.

Each runner isolates optional dependencies so users can install only the
frameworks they need (e.g., wayflow, langgraph) without pulling all of them.
"""

